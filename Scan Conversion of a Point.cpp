#include<bits/stdc++.h>
using namespace std;
int main()
{
    float x,y;
    cout << "Enter point(x,y)" << endl;
    cin >> x>>y;
    int px,py;
    px = floor(x);
    py = floor(y);
    cout << "Scanned points are ("<<px<<","<<py<<")"<<endl;
}
